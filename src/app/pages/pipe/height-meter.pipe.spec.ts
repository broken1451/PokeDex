import { HeightMeterPipe } from './height-meter.pipe';

describe('HeightMeterPipe', () => {
  it('create an instance', () => {
    const pipe = new HeightMeterPipe();
    expect(pipe).toBeTruthy();
  });
});
